задание с версткой сайта:
1) семантика по БЭМ с сохранением GRID
2) комментарии HTML (body, main, footer, области страницы)
3) изображения через CSS
4) все стили в CSS
5) убраны повторяющиеся участки кода (DRY) в HTML и CSS
6) убраны лишние медиа-запросы в CSS (YAGNI) (в старом задание с версткой сайта не было точно 
	указано какие медиа-запросы следует реализовать. сократил их количество здесь ни столько
	в рамках принципа YAGNI, сколько с целью сокращения объема правок).

калькулятор:
1) многие if заменнены на логические операции
2) использована деструктуризация в операциях присваивания

угадайка:
SWITCH заменен на выбор из массива (KISS)

все:
1) использован линтер Beautify (для наглядности сделал отступы в 2 пробела). Под конец выполнения задания
	понял, что нужен линтер "по-мощнее", но оставил как есть.
2) выполнена валидация всех страниц через W3C

по принципам SOLID не делал никаких правок, ибо сами принципы достаточно сложны для понимания и
на момент модуля 15 с использованием объектов и классов в JS выполнено мало заданий, из-за чего
просто мало мест, в которых нужно применить эти принципы. Все задания были несложные, в них нет
наследования классов и каждая сущность и так имеет всего одну ответственность. Допускаю, что я ошибаюсь
и не до конца понял принципы SOLID, но явных нарушений этих принципов в своих заданиях я не нашел.